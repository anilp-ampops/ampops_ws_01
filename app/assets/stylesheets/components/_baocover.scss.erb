.baocover {
  background: rgba(51,51,51,1);
}

.baobckgnd {
  background-image: linear-gradient(rgba(51,51,51,0.8), rgba(51,51,51,1)), url(<%= image_path 'aocoverbck01.png'%>);
  height: 723px;
  background-size: cover !important;
}

.intro-text {
  padding-top: 205px;
  p {
    font-family: $aobantext-font;
    text-shadow: 0 1px black;
    font-style: light;
    font-weight: 300;
    font-size: 24px;
    line-height: 22px;
    align: left;
    color: rgba(255, 255, 255, 0.9);
    padding: 10px;
  }
}

.aocorekw {
  font-weight: 800;
}


.intro-values {
  padding-top: 60px;
}

.introbox-white {
  height: 70px;
  background: rgba(255,255,255,1.0);
  p {
    font-family: $aobantext-font;
    padding-top: 25px;
    font-weight: 400px;
    color: #F2994a
  }
}
